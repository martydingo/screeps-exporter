[project]
name = "screeps-exporter"
version = "0.1.0"
description = ""
authors = [
    {name = "Martin George",email = "marty@dingopaws.net"}
]
readme = "README.md"
requires-python = ">=3.13,<4.0"
dependencies = [
    "screepsapi (>=0.5.1,<0.6.0)",
    "pyyaml (>=6.0.3,<7.0.0)",
    "argparse (>=1.4.0,<2.0.0)",
    "prometheus-client (>=0.23.1,<0.24.0)"
]


[build-system]
requires = ["poetry-core>=2.0.0,<3.0.0"]
build-backend = "poetry.core.masonry.api"
